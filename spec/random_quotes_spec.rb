require 'spec_helper'

describe RandomQuotes do
  it 'has a version number' do
    expect(RandomQuotes::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
